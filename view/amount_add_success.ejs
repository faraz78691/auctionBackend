<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/x-icon" href="http://98.80.36.64/favicon.ico">
  <title>Payment Success</title>
  <style>
    /* General Styling */
    body {
      margin: 0;
      padding: 0;
      font-family: "Nunito Sans", "Helvetica Neue", sans-serif;
      background: linear-gradient(to bottom, #f3f8fd, #eaf1f8);
      color: #404F5E;
      text-align: center;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .card {
      background: #fff;
      border-radius: 10px;
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
      padding: 40px 20px;
      width: 350px;
      text-align: center;
      animation: fadeIn 1s ease-in-out;
    }

    .logo {
      width: 100px;
      height: auto;
      margin: 0 auto 20px;
    }

    h1 {
      color: #4CAF50;
      font-size: 2.5rem;
      margin: 20px 0;
    }

    p {
      font-size: 1.1rem;
      margin: 10px 0;
      line-height: 1.5;
    }

    .icon {
      width: 100px;
      height: 100px;
      border: 5px solid #4CAF50;
      border-radius: 50%;
      margin: 0 auto;
      display: flex;
      justify-content: center;
      align-items: center;
      background: #eaf9e6;
      animation: bounceIn 1.5s;
    }

    .icon i {
      font-size: 3rem;
      color: #4CAF50;
    }

    .btn {
      display: inline-block;
      background: #FF6F61;
      color: #fff;
      padding: 12px 20px;
      border-radius: 5px;
      text-decoration: none;
      font-size: 1rem;
      margin-top: 20px;
      transition: background 0.3s ease;
    }

    .btn:hover {
      background: #ff4c3a;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes bounceIn {
      0% {
        transform: scale(0.5);
      }

      50% {
        transform: scale(1.2);
      }

      100% {
        transform: scale(1);
      }
    }
  </style>
</head>

<body>
  <div class="card">
    <!-- Logo Section -->
    <img src="http://98.80.36.64/assets/img/logo.png" alt="Company Logo" class="logo">
    <!-- Replace `your-logo.png` with the path to your logo -->

    <!-- Success Icon -->
    <div class="icon">
      <i>✓</i>
    </div>

    <!-- Success Message -->
    <h1>Payment Successful</h1>
    <p>
      <%= message %>
    </p>
    <p>Amount: $<%= amount %>
    </p>

    <!-- Button -->
    <a href="http://98.80.36.64/user/fees" class="btn">Go to Your Feed</a>
  </div>
</body>

</html>