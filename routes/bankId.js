const express = require('express');
const bankidController = require('../controller/bankidController');
const router = express.Router();

// router.get('/login', bankidController.login);
// router.get('/callback', bankidController.callback);

module.exports = router;